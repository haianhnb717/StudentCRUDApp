# StudentCRUDApp

C# WinForms + SQL Server demo (CRUD)

## Setup
1. Cài SQL Server (Express hoặc LocalDB)
2. Mở file `Database/StudentDB.sql` trong SSMS và Execute để tạo DB
3. Kiểm tra/sửa `App.config` để khớp Data Source
4. Chạy project trong Visual Studio 2022
